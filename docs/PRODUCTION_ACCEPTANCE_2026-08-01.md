# Production multi-turn Agent acceptance — 2026-08-01

This record covers an owner-authorized production acceptance run against the deployed Paperlight frontend and API. The paper was synthetic and contained no personal, student, course, or institutional data. No credential value, session value, provider key, or paper body is recorded here.

## Environment

- Frontend: `https://k8w98rr595-blip.github.io/academic-writing-agent/`
- API: `https://api-production-840c.up.railway.app`
- Source revision: `4faa372d0901607a09d258d6e47150cfa13c2688`
- Detector: explicitly labeled `Mock Pangram` (`mock-pangram-v3`)
- Rewrite: DeepSeek `deepseek-v4-pro`
- Semantic validator: DeepSeek `deepseek-v4-flash`
- Authentication: owner password; TOTP remained disabled

## Acceptance results

| Check | Result | Evidence |
|---|---|---|
| Production frontend and static application | Pass | GitHub Pages loaded the Paperlight workspace at the repository subpath. |
| Owner login | Pass | The authenticated workspace loaded without reading or recording credential fields. |
| Synthetic document creation | Pass | An 851-word synthetic paper, `Ethics of Data Reuse`, was created. |
| Mock detector labeling | Pass | The report displayed `演示结果，不代表真实服务`, `Mock Pangram`, and `mock-pangram-v3`. |
| Initial risk report | Pass | AI-generated 9.8%, AI-assisted 7.9%, human 82.4%, combined internal risk 17.6%, 11 marked spans. These are deterministic demonstration values, not detector performance evidence. |
| DeepSeek fail-closed behavior | Pass | The first proposal was rejected by semantic-safety validation. A later follow-up that produced no safe distinct change was also rejected without replacing the pending safe proposal. |
| First safe proposal | Pass | A version 1 proposal was generated by `deepseek-v4-pro` and marked as validated by `deepseek-v4-flash`. |
| Successive proposal | Pass | A version 2 proposal was generated in the same rewrite session. The UI retained the immutable original passage while revising the current candidate. |
| Explicit acceptance and immutable version | Pass | Version 2 was accepted by the owner workflow and created document version 2. No proposal was applied automatically. |
| Stale analysis behavior | Pass | Immediately after acceptance, the UI displayed `检测结果需要刷新`; old evidence no longer decorated the edited version. |
| Re-analysis and before/after display | Pass | Mock re-analysis reported 15.9% combined internal risk and displayed `17.6% → 15.9%` with the non-guarantee disclaimer. |
| Desktop layout | Pass | The 1440×1024 workspace exposed document, outline, editor, inspector, version, export, and delete controls without a reported browser error. |
| Mobile layout | Pass | At 390×844, document and body scroll width equaled client width; no horizontal overflow was detected. |
| DOCX export request | Inconclusive | The export control was activated, but the in-app browser did not emit an observable download event before timeout. The request was not repeated. Earlier production acceptance and automated tests cover the DOCX package path, but this run does not independently prove the downloaded payload. |
| Immediate deletion | Pass | After explicit action-time confirmation, the synthetic document tree was deleted. The document library displayed `尚无文稿`. |
| Session logout | Pass | Logout returned the application to the `登录 Paperlight` screen. No credential field value was inspected. |
| Browser console | Pass | No warning or error entries were returned for the acceptance tab. |
| API health | Pass | Direct post-run request returned HTTP 200. |
| Unauthenticated business API | Pass | Direct post-run request to `/api/v1/documents` returned HTTP 401. |
| CORS and HTTPS hardening | Pass | Preflight returned the exact Pages origin and the API returned HSTS with a one-year max age and subdomain coverage. |

## DeepSeek request behavior

Four rewrite-generation actions were submitted during this run:

1. the default improvement request was rejected by semantic-safety validation;
2. a narrow clarity-and-concision request produced proposal version 1;
3. a further request produced no safe distinct change and was rejected;
4. an exact, meaning-preserving transition-word change produced proposal version 2.

Successful proposals exposed the expected Pro and Flash model metadata. No Pangram request was made. The exact DeepSeek charge was not read from an account dashboard, so this record makes no cost claim.

## Residual risks and follow-up

1. Real Pangram remains disabled until the owner supplies a server-side key, accepts the provider's current data-processing terms, approves cost, and completes a synthetic paid acceptance run.
2. TOTP remains disabled. On 2026-08-06 the owner explicitly excluded restoration from the current implementation stage; password-only access is an accepted temporary risk and public/student access remains prohibited.
3. The previously exposed owner password must still be rotated; deploy only its replacement hash and verify prior sessions are rejected.
4. Repeat the DOCX download in a browser surface that exposes the downloaded file, validate the ZIP-based Word package, and remove the local test artifact.
5. AI writing-risk values remain probability-style internal signals. This acceptance does not support accuracy, authorship, misconduct, Turnitin-equivalence, or guaranteed-risk-reduction claims.

## Owner-use conclusion

The deployed Mock-detector plus real-DeepSeek multi-turn workflow is operational for owner-only use. It is not ready for real Pangram use or public/student access. Continued routine owner use remains conditioned on password rotation, and stronger authentication should be restored before access expands.
